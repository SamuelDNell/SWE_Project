const { Given, When, Then } = require('@cucumber/cucumber');

Given('the user is on the chat page', function () {
  // no-op (just for structure)
});

When('the user enters a prompt', function () {
  // no-op
});

Then('the system returns responses from 3 models', function () {
  // fake pass
});

Then('each response should include Model 1, Model 2, and Model 3', function () {
  // fake pass
});
Given('a user sends a message', function () {
  // no-op
});